from itertools import count

numbersList = [1, 2, 3, 4, 5]
for y in range(len(numbersList)):
    print(numbersList[y])
    y += 1
    print(y)
    while y < len(numbersList) and y < 100:
        print(list)
        y += 1
        if y == 100:
            break
        else:
            continue